# Test profile: H2 only (overrides main where keys overlap; include required DB keys)
db.mysql.enabled=false
db.h2.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
db.h2.username=sa
db.h2.password=
spring.h2.console.enabled=false
app.name=task-manager-test
